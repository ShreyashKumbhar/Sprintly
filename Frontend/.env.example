# Leave empty for local dev: Vite proxies /api → http://localhost:8080 (see vite.config.js).
# For production or custom backend URL, set the origin only (no trailing slash):
# VITE_API_URL=https://api.example.com

VITE_API_URL=
